let noisePosition = 0;
let noiseSpeed = 0.01

function setup() {
  createCanvas(windowWidth, windowHeight);
  
}

function draw() {
  background("gray");
  
  noisePosition = 0;
  for 
}

function mousePressed(){
  noisePosition++;
};